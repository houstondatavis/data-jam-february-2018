CREATE TABLE census.item (
	subcategory_code  text,
	item_code         text,
	item_text         text,
	display_level     text,
	selectable        text,
	sort_sequence     int
);

\COPY census.characteristics FROM './data/raw_data/cx.item.txt' WITH ( FORMAT CSV, HEADER true, DELIMITER E'\t' );
